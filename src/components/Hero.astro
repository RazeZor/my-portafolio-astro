---
import { Image } from 'astro:assets';
---

<section class="hero" id="home">
  <div class="container hero-container">
    <div class="hero-content" data-aos="fade-right" data-aos-delay="100">
      <span class="hero-subtitle">Trabajadora Social</span>
      <h1 class="hero-title">Sarai Abigail Hernández Cruzat</h1>
      <h2 class="hero-tagline">Magister en Intervención Familiar</h2>
      
      <p class="hero-description">
        Profesional comprometida con el cambio social, especializada en intervención familiar y comunitaria. 
        Ofrezco asesorías personalizadas para individuos, familias y organizaciones.
      </p>
      
      <div class="hero-buttons">
        <a href="https://calendar.google.com/calendar/u/0/appointments/schedules/AcZssZ2Pm1SpXNlOc_KSlO7PpMbJXk0J_LM0G8yepTI2RYmciLW0A1uij8JgXE6j9Y_6FrLDnRZcnEIP" class="btn" target="_blank">
          <i class="fas fa-calendar-alt"></i> Agendar Consulta
        </a>
        <a href="/CV-Sarai-Hernandez.pdf" class="btn btn-outline" download>
          <i class="fas fa-download"></i> Descargar CV
        </a>
      </div>
    </div>
    
    <div class="hero-image" data-aos="fade-left" data-aos-delay="300">
      <div class="image-container">
        <img src="/img/FotoSari.jpg" alt="Sarai Hernández - Trabajadora Social" width="400" height="400" />
      </div>
      
      <div class="experience-badge" data-aos="fade-up" data-aos-delay="600">
        <span class="years">7+</span>
        <span class="text">Años de<br>Experiencia</span>
      </div>
    </div>
  </div>
  
  <div class="hero-shape"></div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    padding: var(--spacing-xl) 0;
    overflow: hidden;
  }
  
  .hero-container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-lg);
    align-items: center;
    position: relative;
    z-index: 2;
  }
  
  .hero-subtitle {
    display: inline-block;
    font-size: 1.1rem;
    font-weight: 500;
    color: var(--color-primary);
    margin-bottom: var(--spacing-sm);
    padding: 0.3rem 1rem;
    background-color: var(--color-secondary-light);
    border-radius: 50px;
  }
  
  .hero-title {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    color: var(--color-text-dark);
  }
  
  .hero-tagline {
    font-size: 1.5rem;
    color: var(--color-primary);
    margin-bottom: var(--spacing-md);
    font-weight: 500;
  }
  
  .hero-description {
    font-size: 1.1rem;
    margin-bottom: var(--spacing-md);
    max-width: 90%;
    color: var(--color-text-medium);
  }
  
  .hero-buttons {
    display: flex;
    gap: var(--spacing-sm);
    flex-wrap: wrap;
  }
  
  .hero-image {
    position: relative;
    display: flex;
    justify-content: center;
  }
  
  .image-container {
    position: relative;
    width: 400px;
    height: 400px;
    border-radius: 50%;
    overflow: hidden;
    border: 8px solid var(--color-secondary-light);
    box-shadow: var(--shadow-lg);
    z-index: 1;
  }
  
  .image-container img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: var(--transition-slow);
  }
  
  .image-container:hover img {
    transform: scale(1.05);
  }
  
  .experience-badge {
    position: absolute;
    bottom: 10%;
    right: 0;
    background: var(--color-primary);
    color: white;
    width: 120px;
    height: 120px;
    border-radius: 50%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-md);
    z-index: 2;
  }
  
  .experience-badge .years {
    font-size: 2rem;
    font-weight: 700;
    line-height: 1;
  }
  
  .experience-badge .text {
    font-size: 0.9rem;
    text-align: center;
    line-height: 1.2;
  }
  
  .hero-shape {
    position: absolute;
    top: 0;
    right: 0;
    width: 50%;
    height: 100%;
    background-color: var(--color-secondary-light);
    clip-path: polygon(20% 0%, 100% 0%, 100% 100%, 0% 100%);
    z-index: 0;
  }
  
  @media (max-width: 992px) {
    .hero-title {
      font-size: 2.5rem;
    }
    
    .hero-tagline {
      font-size: 1.3rem;
    }
    
    .image-container {
      width: 350px;
      height: 350px;
    }
    
    .experience-badge {
      width: 100px;
      height: 100px;
    }
  }
  
  @media (max-width: 768px) {
    .hero {
      padding-top: 120px;
      min-height: auto;
    }
    
    .hero-container {
      grid-template-columns: 1fr;
      text-align: center;
    }
    
    .hero-content {
      order: 2;
    }
    
    .hero-image {
      order: 1;
      margin-bottom: var(--spacing-md);
    }
    
    .hero-description {
      max-width: 100%;
    }
    
    .hero-buttons {
      justify-content: center;
    }
    
    .image-container {
      width: 280px;
      height: 280px;
    }
    
    .experience-badge {
      bottom: 0;
      right: 50px;
      width: 90px;
      height: 90px;
    }
    
    .experience-badge .years {
      font-size: 1.5rem;
    }
    
    .experience-badge .text {
      font-size: 0.8rem;
    }
    
    .hero-shape {
      width: 100%;
      height: 50%;
      clip-path: polygon(0 0, 100% 0, 100% 100%, 0 70%);
    }
  }
</style>


<section class="hero">
  <div class="container hero-container">
    <div class="hero-content" data-aos="fade-right">
      <h1>Trabajadora Social Comprometida</h1>
      <p>Transformando vidas y construyendo comunidades más fuertes</p>
      <a href="#contact" class="cta-button">Contáctame</a>
    </div>
    <div class="hero-image" data-aos="fade-left">
      <img src="/placeholder-profile.jpg" alt="Trabajadora Social" />
    </div>
  </div>
</section>

<style>
  .hero {
    height: 100vh;
    display: flex;
    align-items: center;
    background-color: var(--color-beige-light);
  }

  .hero-container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    align-items: center;
  }

  .hero-content h1 {
    font-size: 3.5rem;
    color: var(--color-accent);
    margin-bottom: 1rem;
  }

  .hero-content p {
    font-size: 1.2rem;
    margin-bottom: 2rem;
  }

  .cta-button {
    display: inline-block;
    padding: 1rem 2rem;
    background-color: var(--color-accent);
    color: white;
    text-decoration: none;
    border-radius: 5px;
    transition: background-color 0.3s ease;
  }

  .cta-button:hover {
    background-color: var(--color-secondary);
  }

  .hero-image img {
    width: 100%;
    border-radius: 10px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }
</style>
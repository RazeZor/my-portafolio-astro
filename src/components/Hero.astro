
<section class="hero">
  <div class="container hero-container">
    <div class="hero-content" data-aos="fade-right">
      <h1><strong>Trabajadora Social y
        Magister en intervencion familiar</strong></h1>
      <p>profesional con capacidad de aprendizaje, responsabilidad,
        de generar autocritica, trabajo en equipo y liderazgo, habilidades para
        interactuar e intervenir con personas, familias, grupos, organizaciones y
        comunidades</p>
        <a href="#contact" class="cta-button">Horarios</a>
      <a href="#contact" class="cta-button">Contáctame</a>
    </div>
    <div class="hero-image" data-aos="fade-left">
      <img src="img/istockphoto-1719538017-612x612.jpg" alt="Trabajadora Social" />
    </div>
  </div>
</section>

<style>
  .hero {
    height: 100vh;
    display: flex;
    align-items: center;
    background-color: var(--color-beige-light);
  }

  .hero-container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    align-items: center;
  }

  .hero-content h1 {
    font-size: 2.5rem;
    color: var(--color-accent);
    margin-bottom: 1rem;
  }

  .hero-content p {
    font-size: 1.2rem;
    margin-bottom: 2rem;
  }

  .cta-button {
    display: inline-block;
    padding: 1rem 2rem;
    background-color: var(--color-accent);
    color: white;
    text-decoration: none;
    border-radius: 5px;
    transition: background-color 0.3s ease;
  }

  .cta-button:hover {
    background-color: var(--color-secondary);
  }

  .hero-image img {
    width: 100%;
    border-radius: 10px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }
</style>